# Local development
ADMIN_PASSWORD=schimba-parola-admin
PORT=3001

# Gmail SMTP (email confirmare + PDF)
SMTP_USER=adresa@gmail.com
SMTP_PASS=parola-de-aplicatie-gmail
SMTP_FROM="Formular Inscriere <adresa@gmail.com>"
ADMIN_NOTIFICATION_EMAIL=caruntu.emanuel@gmail.com

# Azure Free F1 (SQLite persistent in /home):
# DATA_DIR=/home/data
# NODE_ENV=production
# WEBSITE_NODE_DEFAULT_VERSION=~22
# RESET_DB=true  (doar o dată — șterge baza și repornește ID-urile de la 1; apoi elimină variabila)

# Optional — PostgreSQL (paid on Azure):
# DATABASE_URL=postgresql://user:pass@host:5432/inscrieri?sslmode=require
